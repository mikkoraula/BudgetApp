package addtransaction;

import android.content.Context;
import android.content.res.TypedArray;
import android.util.AttributeSet;
import android.widget.RelativeLayout;
import android.widget.ToggleButton;

import com.example.mikko.budgetapplication.R;

/**
 * Created by Mikko on 20.5.2017.
 *
 * This is a custom view that is used to create a multichoice picker.
 * (For example for when the is creating a new transaction, he can say the transaction is
 * repeated either weekly, monthly or never)
 */

/*
public class MultiChoicePicker extends RelativeLayout {




    public MultiChoicePicker(Context context, AttributeSet attrs) {
        super(context);

        TypedArray a = context.getTheme().obtainStyledAttributes(
                attrs,
                R.styleable.PieChart,
                0, 0);

        try {
            mShowText = a.getBoolean(R.styleable.PieChart_showText, false);
            mTextPos = a.getInteger(R.styleable.PieChart_labelPosition, 0);
        } finally {
            a.recycle();
        }
    }

}

class MultiChoiceToggleButton extends ToggleButton {

    public MultiChoiceToggleButton(Context context, AttributeSet attrs) {
        super(context, attrs);

        TypedArray a = context.getTheme().obtainStyledAttributes(
                attrs,
                R.styleable.PieChart,
                0, 0);

        try {
            mShowText = a.getBoolean(R.styleable.PieChart_showText, false);
            mTextPos = a.getInteger(R.styleable.PieChart_labelPosition, 0);
        } finally {
            a.recycle();
        }
    }
}
*/
